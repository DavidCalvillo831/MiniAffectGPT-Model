# MiniAffectGPT — Lightweight Multimodal Emotion Model

To run:
1. Install requirements  
   `pip install -r requirements.txt`

2. Train  
   `python train.py`

3. Predict  
   `python predict.py`
